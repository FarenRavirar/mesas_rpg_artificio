# Sync ARQUITETURA_PROJETO.md

> Gerado automaticamente após merge em dev
> Data: 2026-04-15T19:14:39.226Z
> Arquivos alterados no merge: 7

---

## Arquivos alterados neste merge

- `.agents/workflows/GIT_WORKFLOW.md`
- `AGENTS.md`
- `BACKLOG_OPERACIONAL.md`
- `FILA_IMPLEMENTACAO.md`
- `OPERACAO_PRODUCAO.md`
- `PRE_DEPLOY_CHECKLIST.md`
- `sessoes/26-04-15_6_auditoria-completa-fila-backlog.md`

---

## Seções do ARQUITETURA_PROJETO.md que precisam de revisão

> **Ação necessária:** Revise cada seção abaixo e atualize
> o ARQUITETURA_PROJETO.md manualmente se necessário.

### §4 Banco de Dados

**O que verificar:** Atualizar lista de migrations aplicadas e estrutura de tabelas.

**Trecho atual no ARQUITETURA_PROJETO.md:**
```
## 4. Modelo de Dados (Entidades Principais)

#
```

---

### §12 Rotas de API

**O que verificar:** Atualizar mapa de rotas — verificar endpoints adicionados ou removidos.

**Trecho atual no ARQUITETURA_PROJETO.md:**
```
## 12. Contratos de API (Rotas Principais)

Referência rápida das rotas estruturais da API. Todas as rotas mutáveis exigem JWT válido no header `Authorization: Bearer <token>`.

### Autenticação
| Método | Rota | Descrição |
|---|---|---|
| `GET` | `/api/v1/auth/google` | Inicia handshake OAuth Google |
| `GET` | `/api/v1/auth/google/callback` | Callback OAuth, retorna JWT |
| `POST` | `/api/v1/auth/logout` | Invalida refresh token |
| `GET` | `/api/v1/me` | Retorna perfil do usuário autenticado |

### Mesas
| Método | Rota | Auth | Descrição |
|---|---|---|---|
| `GET` | `/api/v1/tables` | — | Listagem pública com filtros via query params |
| `GET` | `/api/v1/tables/:slug` | — | Página individual da mesa |
| `POST` | `/api/v1/tables` | `gm` | Criar nova mesa |
| `PUT` | `/api/v1/tables/:id` | `gm` (própria) | Editar mesa |
| `PATCH` | `/api/v1/tables/:id/status` | `gm` / `admin` | Alterar status |
| `POST` | `/api/v1/tables/:id/bookmark` | `player` | Salvar mesa |
| `POST` | `/api/v1/
[... trecho truncado ...]
```

---

### §16 Imagens e Upload

**O que verificar:** Verificar pipeline de upload, campos de URL e campos de deletehash.

**Trecho atual no ARQUITETURA_PROJETO.md:**
```
## 16. Gestão de Imagens (Cloudinary + legado)

#
```

---

### §7 AggregatorBot

**O que verificar:** Verificar pipeline de ingestão, parser Python e campos extraídos.

**Trecho atual no ARQUITETURA_PROJETO.md:**
```
## 7. Funcionalidades por Módulo

#
```

---

## Como aplicar

1. Abra o `ARQUITETURA_PROJETO.md`
2. Navegue até cada seção listada acima
3. Compare com os arquivos alterados e atualize o que estiver desatualizado
4. Feche este PR após aplicar as atualizações necessárias