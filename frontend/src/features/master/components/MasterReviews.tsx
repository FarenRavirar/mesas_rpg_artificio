import type { MasterViewModel } from '../types/masterView.types';

interface MasterReviewsProps {
  vm: MasterViewModel;
}

/**
 * Reviews do mestre (PLACEHOLDER)
 * 
 * Responsabilidades:
 * - Futuro: sistema de avaliações/reviews
 * - Por enquanto: retorna null
 */
export function MasterReviews(_props: MasterReviewsProps) {
  // Placeholder para futuro sistema de reviews
  return null;
}
