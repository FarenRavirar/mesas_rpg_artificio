# ============================================================================
# WORKFLOW: Deploy Production (BREAK-GLASS) — DESATIVADO (mesas no monorepo)
# STATUS: Aposentado. Prod do mesas agora vem do monorepo `artificio`
#         (deploy-mesas.yml mode=deploy / break-glass-deploy-prod.yml).
# MOTIVO: apontava p/ a VM antiga (porta 22 / SERVER_*) recriada na Fase 1 (D036).
# DATA: 2026-06-07
# NOTES: Job desabilitado (if:false). Mantido so como marcador.
# ============================================================================

name: Deploy Production (DESATIVADO — mesas no monorepo artificio)

on:
  workflow_dispatch:

jobs:
  moved:
    if: ${{ false }} # desativado: prod do mesas e pelo monorepo artificio
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: |
          echo "Prod do mesas foi movido para o monorepo 'artificio'."
          echo "Use deploy-mesas.yml (mode=deploy) ou break-glass-deploy-prod.yml no repo artificio."
          exit 1
