# ============================================================================
# WORKFLOW: Promote Beta to Production — DESATIVADO (mesas no monorepo)
# STATUS: Aposentado. A promocao dev->main / prod do mesas agora vem do monorepo
#         `artificio` (promote-prod-fast-forward.yml + deploy-mesas.yml).
# MOTIVO: deployava p/ a VM antiga (porta 22 / SERVER_*) recriada na Fase 1 (D036).
# DATA: 2026-06-07
# NOTES: Job desabilitado (if:false). Mantido so como marcador.
# ============================================================================

name: Promote Beta to Production (DESATIVADO — mesas no monorepo artificio)

on:
  workflow_dispatch:

jobs:
  moved:
    if: ${{ false }} # desativado: promocao/prod do mesas e pelo monorepo artificio
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: |
          echo "Promocao/prod do mesas foi movida para o monorepo 'artificio'."
          echo "Use promote-prod-fast-forward.yml + deploy-mesas.yml no repo artificio."
          exit 1
