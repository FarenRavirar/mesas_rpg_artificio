# ============================================================================
# WORKFLOW: Preflight Prod Gate — DESATIVADO (mesas no monorepo artificio)
# STATUS: Aposentado. O gate de migrations beta/prod do mesas agora vive no
#         monorepo `artificio` (_enforce-migration-dir.yml / pr-checks.yml).
# MOTIVO: validava drift contra a infra antiga (Fase 1 recriou a VM, D036).
# DATA: 2026-06-07
# NOTES: Sem trigger de pull_request (nao roda mais sozinho). Job if:false.
# ============================================================================

name: Preflight Prod Gate (DESATIVADO — mesas no monorepo artificio)

on:
  workflow_dispatch:

jobs:
  moved:
    if: ${{ false }} # desativado: gate de migrations do mesas e pelo monorepo artificio
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: |
          echo "O gate de preflight/migrations do mesas foi movido para o monorepo 'artificio'."
          exit 1
