# ============================================================================
# WORKFLOW: Deploy Beta — DESATIVADO (mesas unificado no monorepo artificio)
# STATUS: Aposentado. O modulo mesas agora deploya pelo monorepo `artificio`
#         (deploy-mesas.yml via _deploy-module.yml, secrets DEPLOY_*).
# MOTIVO: apontava p/ a VM antiga (porta 22 / SERVER_*) recriada na Fase 1 (D036)
#         -> `dial tcp :22 i/o timeout`. Infra morta.
# DATA: 2026-06-07
# NOTES: Sem trigger de push (nao roda mais sozinho). Job desabilitado (if:false).
# ============================================================================

name: Deploy Beta (DESATIVADO — mesas no monorepo artificio)

on:
  workflow_dispatch:

jobs:
  moved:
    if: ${{ false }} # desativado: mesas deploya pelo monorepo artificio
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: |
          echo "Deploy do mesas foi movido para o monorepo 'artificio'."
          echo "Use deploy-mesas.yml (via _deploy-module.yml) no repo artificio."
          exit 1
