# ============================================================================
# WORKFLOW: Docker Cleanup — DESATIVADO (movido)
# STATUS: Aposentado. O projeto mesas foi unificado no monorepo `artificio`.
#         A limpeza periódica de Docker da VM agora vive em:
#         artificio/.github/workflows/docker-cleanup.yml
# MOTIVO: VM única (mesma daemon Docker) — manter o cron aqui duplicaria a
#         limpeza e usaria os secrets antigos (SERVER_*). Fonte única no
#         monorepo, com os secrets canônicos (DEPLOY_*).
# DATA: 2026-06-07
# NOTES: Sem `schedule` (não roda mais sozinho). Job desabilitado por guard.
#        Mantido só como marcador/redirect; pode ser removido no futuro.
# ============================================================================

name: Docker Cleanup (DESATIVADO — movido para artificio)

on:
  workflow_dispatch:

jobs:
  moved:
    if: ${{ false }} # desativado: limpeza agora no monorepo artificio
    runs-on: ubuntu-latest
    steps:
      - name: Aviso
        run: |
          echo "Este workflow foi movido para o monorepo 'artificio'."
          echo "Veja: .github/workflows/docker-cleanup.yml no repositório artificio."
          exit 1
