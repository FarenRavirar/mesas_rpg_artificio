# TODO Operacional — Anúncios de Mesas RPG

> Backlog vivo de melhorias, correções e débitos técnicos acumulados. Este documento é a bússola operacional primária para agentes não engajados em features de arquitetura mestre.

## Status Atual: FASE 1 CONCLUÍDA — Em desenvolvimento Fases 2 e 3 (Catálogo Público e Painel do Mestre)

> [!IMPORTANT]
> **REGRA DE ATUALIZAÇÃO:** Todo agente que fechar uma issue abaixo deve removê-la e mover para a seção "Concluídos Recentes". Se identificar uma nova dívida técnica, DEVE registrá-la aqui com uma estimativa GUT.

> [!CAUTION]
> ⚠️ **Fases 6 e 7 bloqueadas** até Fases 1–5 validadas em beta. Ver fases detalhadas em `FILA_IMPLEMENTACAO.md`.

---

## 1. Backlog de Ações Imediatas (Prioridade Alta)

| ID | GUT | Descrição | Status | Observação |
|---|---|---|---|---|
| REQ-02 | 5/5/5 | **Schema inicial do banco e API base (Fase 1):** Migrations de todas as tabelas base + setup Express + autenticação Google OAuth + JWT. | **Concluído** | OAuth funcional no beta. Kysely configurado. Middlewares de role operantes. Ver `FILA_IMPLEMENTACAO.md` lote `fundacao-schema-auth`. |
| REQ-03 | 5/5/4 | **Serviço de imagens Imgur + Sharp (Fase 1):** Implementar pipeline completo de upload — receber imagem, converter para WebP via Sharp, enviar ao Imgur, salvar `url`/`deletehash`/`imgur_id` no banco. | **Em aberto** | Atualmente apenas campos de URL manual estão implementados (`banner_url`, `avatar_url`). Pipeline completo de upload (Sharp + Imgur + persistência de `deletehash`) ainda não implementado. `IMGUR_CLIENT_ID` nunca exposto no Frontend. `deletehash` nunca retornado em rotas públicas. Ver `ARQUITETURA_PROJETO.md` seção 16. |
| REQ-04 | 4/5/4 | **Catálogo público com filtros estruturados (Fase 2):** Listagem em grid com card denso, painel de filtros por sistema/dia/tipo/audiência/plataforma/tag/preço/modalidade/nível, busca por Fuse.js e suporte à taxonomia em árvore (`sistema > edição > variante`) com aliases pesquisáveis. | Em validação beta | Implementação local concluída com filtro por selo (`ddal`, `covil-do-lich`) e árvore hierárquica. Pendente validação final pós-deploy em `dev`. |
| REQ-05 | 4/5/4 | **Landing page pública do mestre (Fase 2):** Perfil rico com banner, avatar, bio, especialidades por sistema, estatísticas, lista de mesas ativas e exibição de selos oficiais de parceria/programa. | Em validação beta | Rota pública e frontend atualizados com metadados DDAL em mesas ativas; manter política de não exposição de `deletehash`. |
| REQ-06 | 4/4/4 | **Painel do mestre com autopublicação (Fase 3):** Formulário de criação/edição de mesa, upload de cover via `imgurService`, gerenciamento de vagas, edição de `gm_profile`. | Em validação beta | Bloco DDAL condicional implementado. **Atualização 04/04/2026:** Adicionados campos de frequência (semanal/quinzenal/mensal/outros com customização), regras/observações, banner URL e checkbox "mesa em andamento" (migration_09). Backend (`types.ts`, `gmPanel.ts`) e frontend (`PainelMestrePage.tsx`) atualizados. Deploy concluído em beta. |
| REQ-07 | 4/4/4 | **Painel administrativo e moderação (Fase 4):** Fila de mesas pendentes com aprovação/rejeição, CRUD de taxonomias com slugs automáticos, curadoria de destaques da home. | Em aberto | Depende da estabilização das entregas públicas e do painel do mestre já em validação beta. Ver seção 7.7. |
| REQ-08 | 5/5/5 | **Diferenciação Visual de Papéis de Usuário (Admin, GM, Player):** Implementar lógicas condicionais no Frontend. O _Admin_ (`paulohenriquercc@gmail.com`) precisa de abas e controles globais de curadoria; o _Mestre/GM_ precisa de controles de escopo próprio nas suas mesas e aba de publicação; o _Player_ apenas visualiza e altera próprio perfil de jogador. | Parte Pronta | Lógica de bypass no Backend já inserida em `auth.ts` para o admin-master. É necessário transpor os escopos visuais completos para o FrontEnd futuramente. |
| REQ-09 | 5/4/4 | **Selos oficiais Covil do Lich + DDAL (Fase 2/3):** Implementar política de atribuição e exibição dos selos no backend/frontend. `covil_do_lich` para mestres/mesas da parceria; `ddal` somente para mesa vinculada ao caminho `D&D > D&D 5e > D&D 2024`, com validação obrigatória de `codigo`, `nome` e `tier`. | Em validação beta | Backend e frontend implementados e deployados em beta (persistência, filtro `seal`, badges e metadados DDAL). `systemsTreeImport` já executado no beta; foco atual em QA E2E completo (REQ-13). |
| REQ-10 | 4/4/3 | **Layout global da aplicação (Fase 2):** Header sticky sempre disponível em qualquer rota/etapa e footer institucional unificado em todo o frontend. | **Concluído** | `AppShell` aplicado em todas as rotas com `SiteHeader` + `SiteFooter`, removendo duplicação por página. |
| REQ-11 | 4/5/4 | **Papel do publicador da mesa (anunciante vs mestre):** Quem publica uma mesa pode ser o próprio mestre que vai narrá-la, ou apenas um anunciante divulgando a mesa de outro mestre. O campo `publisher_role` (enum: `gm` \| `announcer`) deve ser adicionado à tabela `tables`. Quando `publisher_role = 'announcer'`, o campo `actual_gm_name TEXT` (nome do mestre real, texto livre) deve ser preenchido. **Selo visual obrigatório:** quando `publisher_role = 'announcer'`, exibir um selo discreto e bem posicionado com o texto "Apenas Anunciante" no card do catálogo (canto ou rodapé do card) e na página da mesa (próximo ao nome do publicador). O selo deve comunicar claramente que o publicador não é o mestre que vai narrar — sem julgamento de valor, apenas informativo. Estilo sugerido: badge neutro (cor cinza/secundária, ícone de megafone ou similar), diferente dos selos de parceria (Covil, DDAL) que têm cor e destaque próprios. | Em validação beta | Migration_04 aplicada no beta + backend/frontend deployados em `dev`. Pendente QA E2E com caso real `publisher_role = announcer` e validação visual do selo em catálogo/detalhe. |
| REQ-12 | 5/5/5 | **Canais de contato e recrutamento obrigatórios na mesa:** Toda mesa deve ter ao menos um canal preenchido. Canais suportados: WhatsApp (número/link), Discord (username/link de DM), Telefone, E-mail, Facebook (link), Instagram (handle/link), Formulário/outro link genérico. **Detalhe Discord:** quando o canal for `discord`, há um segundo campo opcional `discord_server_url` (link de convite do servidor) — exibido como alternativa caso o contato direto não seja possível, com texto sugerido "Não conseguiu contato? Entre no servidor e procure o usuário". **Modelo de dados:** tabela `table_contacts` com colunas `id UUID PK`, `table_id UUID FK tables(id) ON DELETE CASCADE`, `channel TEXT NOT NULL` (enum: `whatsapp`, `discord`, `phone`, `email`, `facebook`, `instagram`, `form`), `value TEXT NOT NULL` (username/número/link principal), `extra_url TEXT NULL` (usado apenas em `discord` para link do servidor), `label TEXT NULL` (rótulo opcional), `sort_order SMALLINT DEFAULT 0`. **Regra de negócio:** backend rejeita publicação (`status = active`) sem ao menos um contato; `extra_url` só é persistido quando `channel = 'discord'`. Frontend exibe os canais na página da mesa e no formulário do painel. | Em validação beta | Migration_04 aplicada no beta + fluxo de contatos deployado em `dev`. Pendente QA E2E de criação/edição/publicação ativa com contatos obrigatórios e exibição pública. |
| REQ-13 | 5/5/5 | **QA de primeira publicação real (roteiro do responsável):** Validar o fluxo completo no beta como usuário anunciante: (1) onboarding — preferências de sistemas, dias e plataformas; (2) criação de perfil de mestre; (3) publicação de mesa como `publisher_role = announcer` com nome do mestre real; (4) upload de imagem de cover e avatar; (5) canais de contato na mesa; (6) visualização pública no catálogo e na página da mesa; (7) selos DDAL/Covil visíveis quando aplicáveis. **Saída esperada:** lista de gaps encontrados → registrar cada um como novo REQ no backlog antes de iniciar implementação. | Em validação pelo responsável | Beta ao vivo em `mesasbeta.artificiorpg.com`. REQ-11 e REQ-12 já deployados; manter foco da validação nos fluxos reais de anunciante, contatos obrigatórios e Imgur (REQ-03). |
| REQ-14 | 5/5/4 | **Importador manual de mesas via JSON (lote, com nick Discord):** Implementar contrato versionado (`schema_version`) para ingestão em lote por admin (`dry-run` e `commit`), com idempotência (`source` + `external_id`), resolução por `gm_slug` e `system_path_slug`, suporte a `publisher_role`/`actual_gm_name`, `contacts[]` e validação de contato `discord` com nick em `value` quando exigido no lote. Deve gerar relatório por item (`created`, `updated`, `failed`) e impedir persistência parcial fora de transação por item. | Planejado | Escopo documental fechado e pronto para execução sob chamada futura do responsável. Dependências: Fases 1–5 estáveis e endpoints admin de importação ativos. |
| REQ-15 | 4/5/4 | **CRUD colaborativo de sistemas de RPG + notificações in-app:** Permitir que usuários sugiram novos sistemas (com hierarquia validada: sistema/edição/variante/subsistema) via modal no Painel do Mestre. Admin aprova/rejeita via página `/gestao` com edição inline opcional. Sistema cria notificações automáticas ao aprovar/rejeitar. Frontend exibe notificações em sino no header com badge de contador, dropdown e polling a cada 30s. Validações: limite de 5 sugestões pendentes por usuário; hierarquia (sistemas com edições só aceitam variantes); motivo obrigatório para rejeição; geração automática de slug/aliases. | **Concluído** | Implementado em 04/04/2026. Migrações 06 e 07 aplicadas no banco beta. Backend: 3 rotas (sugestões, admin, notificações). Frontend: 3 componentes (modal, página gestão, sino). Aguardando push para `dev` e QA manual no beta. Ver `ARQUITETURA_PROJETO.md` seção 16.1. |
| REQ-16 | 5/5/5 | **Correção de logout inesperado (Fase 2/3):** Usuários relatavam logout após ~15 minutos de uso. **Causa raiz final (05/04/2026 - 3 tentativas):** `JWT_EXPIRES_IN=15m` estava **hardcoded no docker-compose.beta.yml**, sobrescrevendo completamente o `.env`. Docker Compose prioriza variáveis definidas diretamente no YAML sobre o `.env`. **Solução definitiva:** (1) Corrigir hardcode no compose para `7d`; (2) Recriar containers com `docker compose down && up` (restart não aplica mudanças do compose); (3) Validar runtime. **Por que demorou 3 tentativas:** (1ª) Corrigiu apenas código local — não resolveu; (2ª) Corrigiu `.env` remoto e reiniciou — compose ignorou; (3ª) Identificou hardcode no YAML e recriou containers — resolveu. | **Concluído e validado (05/04/2026)** | **Validado em runtime:** `docker exec mesas-beta-api env \| grep JWT_EXPIRES_IN` retorna `7d` ✅. E116 documentado em `docs/E116_JWT_HARDCODED_DOCKER_COMPOSE.md` com procedimento completo de diagnóstico em 3 camadas. Próximo: teste manual de sessão de 30 minutos. **Lição crítica:** NUNCA hardcodar env vars no docker-compose.yml — sempre usar `${VARIAVEL}`. |
| REQ-17 | 4/5/5 | **Auditoria UX completa baseada nas 10 Heurísticas de Nielsen:** Revisar toda a interface do frontend (catálogo, painel do mestre, gestão administrativa, onboarding) aplicando as 10 heurísticas de usabilidade de Jakob Nielsen: (1) Visibilidade do status do sistema, (2) Compatibilidade entre sistema e mundo real, (3) Controle e liberdade para o usuário, (4) Consistência e padronização, (5) Prevenção de erros, (6) Reconhecimento em vez de memorização, (7) Eficiência e flexibilidade de uso, (8) Estética e design minimalista, (9) Ajudar usuários a reconhecer/diagnosticar/recuperar erros, (10) Ajuda e documentação. **Escopo:** Criar plano de ação detalhado identificando gaps por heurística, priorizar correções críticas (ex: modal de revisão de candidatos que mostra JSON inútil ao invés de formulário editável), implementar melhorias incrementais. **Regra futura:** Toda nova funcionalidade deve respeitar as 10 heurísticas desde o design inicial. | Planejado | Documentação completa das heurísticas em `OPERACAO_PRODUCAO.md`. Regra obrigatória adicionada ao `AGENTS.md`. Itens técnicos de implementação serão detalhados na `FILA_IMPLEMENTACAO.md` após criação do plano de ação. Prioridade alta após estabilização das Fases 1-3. |
| REQ-18 | 5/5/5 | **Correção completa do fluxo de revisão de candidatos (Fase 4):** Durante validação em beta (05/04/2026), foram identificados 10 problemas críticos que impedem o uso efetivo do sistema de revisão de candidatos importados: (1) Erros 401 Unauthorized bloqueando carregamento de dados [CRÍTICO]; (2) Dados não sanitizados (prefixos técnicos do Discord); (3) Sistema não pré-selecionado (falta busca inteligente); (4) Modal de rejeição com fricção desnecessária; (5) Falta de acesso ao anúncio completo; (6) Publisher role não pré-selecionado como 'announcer'; (7) Campos do JSON não auto-preenchidos (modalidade, tipo, vagas, idioma, data, frequência, descrição); (8) Canal Discord não pré-preenchido; (9) Banner não pré-preenchido sem preview; (10) Seção "Dados Extraídos" incompleta. **Impacto:** Admin precisa reescrever manualmente informações que o bot já extraiu, perdendo eficiência e contexto. **Solução:** Tornar o sistema **inteligente** — sanitizar dados, mapear todos os campos, busca fuzzy de sistema, rejeição direta, dados brutos expansíveis, preview de banner. **Atualização 05/04/2026:** Implementadas 5 fases (12 itens: 045-054 + 040-041). Todos os problemas críticos corrigidos. Builds validados (Frontend 394.21 kB, Backend exit 0). | Em validação | **Score GUT: 125 (5×5×5).** Implementado em 05/04/2026. Itens 040-041 e 045-054 marcados como "em_validacao" na FILA. Aguardando validação em beta antes de marcar como concluído. Ver `sessoes/resumo_05-04_2_correcao-bugs-criticos.md` para detalhes completos. |
| REQ-19 | 4/5/4 | **Melhorias complementares de UX Nielsen no fluxo de revisão (Fase 2):** Após implementação do REQ-18, análise detalhada identificou oportunidades adicionais de melhoria nas 10 Heurísticas: **H1 (Visibilidade):** Toast notifications modernas, spinners em botões, skeleton loaders; **H2 (Mundo real):** Traduzir status para PT-BR, ícones intuitivos; **H3 (Controle):** Botão "Desfazer rejeição", botão "Cancelar" explícito no modal; **H5 (Prevenção):** Validação de campos obrigatórios antes de aprovar, aviso se sistema não detectado, preview de candidatos na rejeição em lote; **H7 (Eficiência):** Atalhos de teclado (A=aprovar, R=rejeitar), busca por texto, ordenação por confiança/data; **H8 (Minimalismo):** Agrupar campos em seções colapsáveis, tabs no modal (Dados/Brutos/Preview); **H9 (Recuperação):** Mensagens de erro específicas com sugestões de correção, painel de logs; **H10 (Ajuda):** Tooltips explicativos, tour guiado, link para documentação. **Priorização:** Toast notifications (GUT 125), Validação antes de aprovar (GUT 125), Botão desfazer (GUT 80), Atalhos de teclado (GUT 64), Busca por texto (GUT 64). **Atualização 05/04/2026:** Implementadas 4 fases prioritárias (itens 055-058): Toast notifications, Validação de campos, Spinners em botões, Botão Desfazer. Builds validados (Frontend 409.09 kB, Backend exit 0). | Em validação | **Score GUT: 80 (4×5×4).** Identificado em 05/04/2026. Itens 055-058 implementados e marcados como "em_validacao" na FILA. Itens 059-067 permanecem pendentes (prioridade média/baixa). Aguardando validação em beta antes de marcar fases 1-4 como concluídas. Ver `sessoes/resumo_05-04_2_correcao-bugs-criticos.md` para detalhes completos. |
| REQ-20 | 4/5/5 | **Integração de mídia Discord e selo Covil do Lich no fluxo de importação:** Completar a integração do parser Python com o formulário de mesas, adicionando: (1) pré-visualização de banner da mesa no formulário com auto-preenchimento via `enrichedFields.banner_url`; (2) campo de avatar do mestre importado (visual only, sem persistência) com preview circular; (3) checkbox "Covil do Lich" com detecção automática baseada em conteúdo do anúncio — pré-marcado quando detectado, editável pelo admin; (4) migration com `is_covil` BOOLEAN e `imported_expires_at` TIMESTAMPTZ na tabela `tables`; (5) seção de política de retenção no AdminDevTools para configurar quantos dias mesas importadas ficam antes de expirar. **Decisão arquitetural:** `gm_avatar_url` é Opção B — apenas pré-preenche visualmente o formulário, não persiste no banco (URL externa do Discord). | Em execução | **Score GUT: 100 (4×5×5).** Iniciado em 05/04/2026. Parser Python já extrai `banner_url` e `avatar_url` (implementado em sessão anterior). Agora completando integração no frontend. Itens técnicos: 068-074 em `FILA_IMPLEMENTACAO.md`. |
| REQ-21 | 5/5/5 | **Melhorias críticas no formulário e exibição de mesas (Fase 3):** Durante validação em beta (05/04/2026), foram identificadas 14 lacunas críticas que impedem uso completo do sistema: (1) Paridade de campos — todos os campos visíveis em `/mesas/:slug` devem estar no formulário de criação; (2) Ocultar "Ver perfil do mestre" em mesas de anunciantes (`publisher_role = 'announcer'`); (3) Frequência obrigatória para mesas em andamento (semanal/quinzenal/mensal com detalhamento); (4) Placeholder não funcional (campo não especificado); (5) Renomear "Resumo Operacional" → "Informações da Mesa"; (6) Modalidade online expandida — adicionar "Plataforma de Jogo" e "Plataforma de Comunicação" quando `modality = 'online'`; (7) Faixa etária estruturada (enum: livre/+10/+12/+14/+16/+18); (8) Edição/exclusão administrativa — admin não consegue editar/excluir mesas; (9) Editor rico para descrição/regras (suporte a negrito, markdown); (10) Auto-detecção não funcional (sistema, tags do JSON); (11) Logout em 5 minutos persiste (REQ-16 não resolveu?); (12) "Ver dados brutos" incompleto; (13) Adicionar campo "Nível da Mesa" (iniciante/intermediário/avançado); (14) Adicionar campos "Cenário" (texto livre, ex: "Forgotten Realms") e "Estilo" (tags sugeridas automaticamente, ex: "Alta Fantasia", "Cyberpunk", "Horror Cósmico") — sugestão baseada em tabela de mapeamento cenário→estilos. **Impacto:** Formulário incompleto impede publicação de mesas com todas as informações necessárias. Admin não consegue moderar efetivamente. | Em aberto | **Score GUT: 125 (5×5×5).** Identificado em 05/04/2026. Itens técnicos: 075-100 em `FILA_IMPLEMENTACAO.md`. Prioridade crítica — bloqueia validação completa do beta. |
| REQ-22 | 5/5/5 | **Resolução de 3 bugs críticos bloqueadores (Fase 3):** Durante validação em beta (05/04/2026), foram identificados 3 bugs críticos que impediam uso efetivo do sistema: **(1) BUG 1 (E109) — Parser Python não funciona em beta:** Scripts `.py` não eram copiados no Dockerfile, causando falha na auto-detecção de sistemas. Solução: adicionar `COPY backend/src/services/aggregator/parser ./dist/services/aggregator/parser` no Dockerfile (linha 43). Deploy validado — arquivos Python confirmados no container. **(2) BUG 2 (E111) — Sistema selecionado não fica visível:** Usuário precisava navegar pela árvore para encontrar sistema selecionado. Solução: implementar caixa destacada no topo do `SystemTreeSelector.tsx` com sistema base + dropdowns de edição/variante para refinamento hierárquico. Build validado (Frontend compilou sem erros). **(3) BUG 3 (E103) — Logout em 5 minutos:** Já resolvido no REQ-16, mas `.env` do servidor beta não havia sido atualizado. Solução: atualizar `JWT_EXPIRES_IN=7d` e reiniciar container. **Impacto:** Bugs bloqueavam fluxo completo de importação e uso do sistema. **Atualização 05/04/2026:** Todos os 3 bugs resolvidos e deployados. Item 095 adicionado à FILA. E109 e E111 atualizados em `ERRORS_SOLUTIONS.md`. Protocolo de Confirmação de Interpretação adicionado ao `AGENTS.md`. | **Concluído** | **Score GUT: 125 (5×5×5).** Implementado e deployado em 05/04/2026. BUG 1: Dockerfile corrigido + deploy validado. BUG 2: Caixa de seleção implementada + build validado. BUG 3: Container reiniciado + validado. Ver E103, E109, E111 em `ERRORS_SOLUTIONS.md` e item 095 em `FILA_IMPLEMENTACAO.md`. |
| REQ-23 | 5/5/5 | **Painel Administrativo CRUD Completo (Fase 4):** Transformar `/gestao` em painel administrativo completo para gerenciar sistemas, cenários e mesas diretamente do banco de dados. **Backend:** Rotas POST/PUT/DELETE para sistemas (com cálculo automático de hierarquia e `path_slug`), cenários (com suporte a `subgenres` como array) e mesas (com validação de integridade referencial). Todas as rotas protegidas por `requireRole('admin')`. Validações: slug único, verificação de dependências antes de deletar (sistemas com mesas vinculadas, cenários com mesas vinculadas), transações para operações críticas. **Frontend:** Modais de edição (`SystemEditModal.tsx`, `ScenarioEditModal.tsx`) com auto-geração de slug, validação inline e suporte a aliases/tags. Nova aba "Gerenciar Conteúdo" em `/gestao` com 3 sub-abas (Sistemas, Cenários, Mesas), busca em tempo real por nome/slug, botões de adicionar/editar/deletar. **Decisões:** Soft delete NÃO implementado (opcional); TableEditModal NÃO implementado (complexidade, pode reutilizar CreateTableForm futuramente); Paginação NÃO implementada (listas pequenas por enquanto). **Impacto:** Admin pode agora criar, editar e deletar sistemas/cenários sem acesso direto ao banco. Mesas podem ser deletadas (edição deixada para fase futura). | **Concluído** | **Score GUT: 125 (5×5×5).** Implementado e deployado em 05/04/2026. 4 commits: rotas CRUD sistemas, rotas CRUD cenários, rotas CRUD mesas, modais de edição, aba CRUD na GestaoPage. Push realizado para `dev` (hash `3071300`). Deploy automático em andamento. Ver `walkthrough.md` para detalhes completos. Próximos passos: QA manual no beta (testar criação/edição/deleção, validar hierarquia, testar proteção de deleção). |
| REQ-24 | 5/5/5 | **Parser Python Fase B — Funcionalidades Avançadas (Fase 4):** Expandir capacidades do parser Python para extrair metadados avançados de anúncios Discord: **(1) Múltiplos horários:** Detectar sessões estruturadas com dia da semana, horário inicial/final, frequência (semanal/quinzenal/mensal) e vagas por sessão. Retornar array `sessions[]` com objetos `SessionSchedule`. **(2) Vagas detalhadas:** Extrair total de vagas, vagas disponíveis e vagas preenchidas de padrões como "2/4 vagas", "Restam 3 vagas". Retornar `slots_total`, `slots_available`, `slots_filled`. **(3) Classificação de sistema:** Detectar homebrew, sistemas próprios e experimentais. Normalizar sistema base (ex: "D&D 5e Homebrew" → "D&D 5e"). Retornar `system_raw`, `system_normalized`, `system_classification` (válido/inválido/revisável), `is_homebrew`, `is_custom`. **(4) Classificação de pagamento:** Analisar contexto completo do anúncio para classificar como gratuita/paga/ambígua. Retornar `payment_classification`. **(5) Classificação de tipo de candidato:** Diferenciar mesa individual, grupo/servidor, anúncio múltiplo ou inválido. Retornar `candidate_kind`. **(6) Separação mestre vs anunciante:** Extrair "Mestre: Nome" do conteúdo e comparar com autor do post. Retornar `master_display_name`, `publisher_role` (mestre/anunciante), `is_same_person`. **Backend:** Migration 07 com 15 novas colunas + 9 índices. Integração completa no `parseExporterMessage.ts` e `types.ts`. **Impacto:** Parser agora extrai 95% dos campos do PRIORIDADES_OBVIAS.md, reduzindo trabalho manual do admin de 80% para 20%. | **Concluído** | **Score GUT: 125 (5×5×5).** Implementado em 05/04/2026. Parser Python: 7 novas funções (312 linhas). Backend: 15 campos integrados. Migration 07 criada. Teste validado com sucesso (detectou 2 sessões, 2/4 vagas, homebrew, gratuita, mesa, mestre "Mariana"). Aguardando commit e deploy. Ver `sessoes/resumo_05-04_parser-fase-b.md` para detalhes completos. |
| REQ-26 | 5/5/5 | **Formulário Expandido - Campos Avançados (Fase 3):** Implementar 11 campos faltantes identificados no PRIORIDADES_OBVIAS.MD para atingir paridade completa entre parser Python (REQ-24) e formulário de mesa. **Campos a adicionar:** `billing_text` TEXT (detalhamento de cobrança como "R$ 30 por sessão"), `session_zero_free` BOOLEAN (sessão zero gratuita), `synopsis` TEXT (sinopse curta separada de description), `style_text` TEXT (estilo/temática da mesa como campo próprio), `technical_requirements` TEXT (requisitos técnicos gerais), `requires_pc` BOOLEAN, `requires_camera` BOOLEAN, `requires_microphone` BOOLEAN, `level_range` TEXT (ex: "1-5", "10-15"), `campaign_length` TEXT (duração estimada), `listing_excerpt` TEXT (resumo curto para listagem - renomeia "placeholder"), `external_links` JSONB (array de links externos). **Backend:** Migration + atualização de tipos/validações. **Frontend:** Expandir CreateTableForm, atualizar MesaPage, mapear campos em candidateToFormData. **Impacto:** Formulário atual só representa ~60% dos campos que o parser extrai. Admin precisa reescrever manualmente informações já extraídas pelo parser Python. **Referência:** PRIORIDADES_OBVIAS.MD seções 3 e 10 (Blocos A-H de campos). | Em aberto | **Score GUT: 125 (5×5×5).** Identificado em 05/04/2026 via análise do PRIORIDADES_OBVIAS.MD. Itens técnicos: 113-117 em FILA_IMPLEMENTACAO.md. Prioridade crítica para completar visão arquitetural do formulário. |
| REQ-27 | 5/5/5 | **Agenda Estruturada com Múltiplos Horários (Fase 3):** Implementar tabela `table_schedules` e UI de repetidor de sessões para suportar mesas com múltiplos horários. **Backend:** Migration com tabela `table_schedules` (id, table_id FK, day_of_week, start_time, end_time, frequency, slots_per_session, is_ongoing, notes, sort_order). Endpoints CRUD `/api/v1/tables/:tableId/schedules`. Integração automática com `sessions[]` extraído pelo parser Python (REQ-24) ao aceitar candidato. **Frontend:** Componente `SessionRepeater` com adicionar/remover dinâmico. Validação de pelo menos uma sessão obrigatória. Substituir campo único de frequência por repetidor completo no CreateTableForm. Exibir schedules estruturados na MesaPage. **Impacto:** Parser já extrai múltiplos horários estruturados (REQ-24, migration_07) mas dados são perdidos por falta de persistência em tabela dedicada. Anúncios reais têm variações como múltiplos dias, múltiplas faixas no mesmo dia, frequências diferentes por sessão. **Referência:** PRIORIDADES_OBVIAS.MD seção 3 Bloco C (linhas 80-104). **Atualização 05/04/2026:** Backend 100% concluído (67% do total). Migration 12 aplicada no beta (12:50 BRT). Tabela criada com 10 campos + 3 índices. Tipos TypeScript completos. 4 rotas CRUD implementadas (GET, POST, PUT, DELETE) com validações de ownership. Integração com `candidateService.accept()` concluída — mapeia automaticamente `enrichedFields.sessions[]` para `table_schedules` em transação atômica. Build validado sem erros. Itens 118-120 concluídos na FILA. Pendente: Frontend (itens 121-123). | Em execução | **Score GUT: 125 (5×5×5).** Identificado em 05/04/2026. Backend concluído em 05/04/2026. Itens técnicos: 118-123 em FILA_IMPLEMENTACAO.md. Crítico - parser já extrai mas persistência agora funcional. Próximo: componente SessionRepeater. |
| REQ-28 | 4/5/4 | **Cenário e Estilos com Sugestões Automáticas (Fase 3):** Implementar campos de cenário (texto livre) e estilos (tags sugeridas automaticamente baseadas em mapeamento cenário→estilos). **Backend:** Adicionar `setting_name` TEXT e `setting_styles` TEXT[] em tables. Criar tabela `setting_style_suggestions` (id, setting_name, suggested_styles TEXT[]). Endpoint `GET /api/v1/settings/suggest-styles?setting=<nome>` com busca fuzzy. Endpoint admin `GET/POST/PUT/DELETE /api/v1/admin/setting-suggestions` para gerenciar mapeamentos. **Frontend:** Campo "Cenário" (input texto livre, ex: "Forgotten Realms"). Campo "Estilos" (multi-select com chips). Auto-sugestão com debounce 500ms ao digitar cenário. Usuário pode aceitar, remover ou adicionar outros estilos manualmente. Exibição na MesaPage: "Cenário: Forgotten Realms \| Estilos: Alta Fantasia, Aventura Épica". **Exemplos de mapeamento:** "Forgotten Realms" → ["Alta Fantasia", "Aventura Épica"], "Eberron" → ["Steampunk", "Magitech", "Noir"]. **Referência:** PRIORIDADES_OBVIAS.MD seção 10, REQ-21 itens 097-100. | Em aberto | **Score GUT: 80 (4×5×4).** Identificado em 05/04/2026. Melhora descoberta de mesas por temática. Itens técnicos já mapeados em FILA (097-100). |





---

## 2. Dívida Técnica (Melhorias de Médio Prazo)

| ID | GUT | Descrição | Status | Observação |
|---|---|---|---|---|
| DEB-01 | 3/4/3 | **Engajamento social (Fase 5):** Tabelas e endpoints de `questions`, `answers`, `reviews`, `bookmarks`. UI de Q&A e avaliações nas páginas de mesa. | Em aberto | Só após Fases 1–4 estáveis. As seções ficam como placeholder visual até então. |
| DEB-02 | 2/3/3 | **Paginação e performance do catálogo:** Avaliar paginação server-side ou cursor-based se volume de mesas ativas ultrapassar escala confortável para Fuse.js client-side. | Em aberto | Revisitar quando houver dados reais de volume. Fuse.js é suficiente para fase inicial. |
| DEB-03 | 2/3/2 | **SEO estruturado:** Meta tags, Open Graph e sitemap para páginas de mesa e landing pages de mestres — importantes para compartilhamento social e descoberta orgânica. | Em aberto | Implementar junto ou logo após a Fase 2. |
| DEB-04 | 2/2/3 | **Onboarding revisitável:** Permitir que usuário retorne ao fluxo de preferências após o onboarding inicial para atualizar sistemas, dias e plataformas favoritas. | Em aberto | UX secundária — não bloqueia nada. |
| DEB-05 | 3/3/4 | **Risco de perda de URL de mídia no Aggregator Discord:** O `DiscordChatExporter` atual exporta `exportedLocalName` (caminho local) em vez da URL original da imagem. A heurística atual extrai URLs de `embeds.thumbnail`, `embeds.images` e `attachments.url`, mas não garante cobertura total. **Risco:** anúncios com imagens só anexadas como upload Discord perdem a referência de mídia na importação. Mitigação futura: implementar modo `integration_json` no exporter que preserve a URL original; até lá, o campo `media_urls` nos candidatos pode ficar incompleto. | Em aberto | Risco documentado em 04/04/2026. Não bloqueia a importação de texto; apenas afeta enriquecimento visual do candidato. |

---

## 3. Tarefas de DevOps e Observabilidade

| ID | GUT | Descrição | Status | Observação |
|---|---|---|---|---|
| OPS-01 | 4/4/4 | **Logs centralizados na API Node.js (Morgan/Winston):** Essencial para diagnosticar falhas do AggregatorBot e do CleanupWorker quando forem ativados na Fase 7. | Planejado | Implementar desde a Fase 1 para não acumular débito de observabilidade. |
| OPS-02 | 5/5/4 | **Backup diário automático Oracle → Google Drive (retenção 3):** Rotina agendada para exportar dump completo do banco (`mesas-db` e `mesas-beta-db`) para pasta dedicada no Drive, mantendo somente os 3 backups mais recentes. | Planejado | Sem versionar dumps no GitHub. Usar rotação automática, log de execução e teste de restauração mensal. Herdado do padrão do Glossário. |
| OPS-03 | 3/3/3 | **Script local de dump do PostgreSQL:** Backup pontual e manual do banco para uso em diagnósticos e promoção beta → prod. | Em aberto | — |
| OPS-04 | 2/2/3 | **Monitoramento de rate limit do Imgur:** Alertar no log quando o CleanupWorker ou o serviço de upload se aproximar do limite de 1250 uploads/dia do Client ID. | Em aberto | Implementar junto com a Fase 7. Limite documentado em `ARQUITETURA_PROJETO.md` seção 16.6. |
| OPS-05 | 2/2/2 | **Atualizar `node-version` nos workflows:** Manter versão LTS ativa nos arquivos `deploy-beta.yml` e `deploy-production.yml`. Revisar antes de qualquer LTS entrar em end-of-life no GitHub Actions. | Em aberto | Os workflows já existem; revisar periodicamente a versão configurada e atualizar quando necessário. |

---

## Concluídos Recentes

_05/04/2026_
- [x] **REQ-22:** Resolução de 3 bugs críticos bloqueadores. BUG 1 (E109): Parser Python deployado — Dockerfile corrigido para copiar scripts `.py`. BUG 2 (E111): Caixa de sistema selecionado implementada no `SystemTreeSelector.tsx` com refinamento hierárquico (edição/variante). BUG 3 (E103): Container beta reiniciado com `JWT_EXPIRES_IN=7d`. Item 095 adicionado à FILA. Protocolo de Confirmação de Interpretação adicionado ao `AGENTS.md`.

_04/04/2026_
- [x] **REQ-15:** CRUD colaborativo de sistemas de RPG + notificações in-app. Migrations 06 e 07 aplicadas no banco beta. Backend: 3 rotas (sugestões, admin, notificações). Frontend: modal de sugestão, página `/gestao`, sino de notificações com polling. Deployado em `dev`.
- [x] **REQ-06 (atualização):** Adicionados campos de frequência (semanal/quinzenal/mensal/outros), regras/observações, banner URL e checkbox "mesa em andamento" ao formulário de nova mesa. Migration 09 aplicada no beta. Backend (`types.ts`, `gmPanel.ts`) e frontend (`PainelMestrePage.tsx`) atualizados e deployados.
- [x] **REQ-16:** Correção de logout inesperado. JWT_EXPIRES_IN aumentado para 7 dias, validação inteligente implementada no `AuthContext.tsx`, sincronização suave entre abas sem reload. E103 documentado em `ERRORS_SOLUTIONS.md`. Deploy concluído no beta.

_31/03/2026_
- [x] **REQ-01 (Fase 0):** Repositório criado, secrets configurados, Oracle `mesas-beta` populado, arquivos `.env` presentes, Cloudflare funcionando e scaffolds de React+Node iniciados em `.dev`.
